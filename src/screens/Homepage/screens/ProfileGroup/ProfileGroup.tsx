import React from 'react';
import {AppSafeAreaView, AppText} from '@/components';

export default function ProfileGroup() {
  return (
    <AppSafeAreaView backgroundColor="bgBlack" flex={1}>
      <AppText>ProfileGroup</AppText>
    </AppSafeAreaView>
  );
}
